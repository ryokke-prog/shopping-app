# お買いもの アプリ仕様書

## 概要

パートナーと買い物リストをリアルタイムで共有できるPWA（Progressive Web App）。
Netlifyでホスティングし、iPhoneのホーム画面にアプリとして追加できる。

---

## 技術スタック

| 項目 | 内容 |
|------|------|
| フロントエンド | HTML / CSS / Vanilla JS（単一ファイル） |
| データベース | Firebase Realtime Database |
| ホスティング | Netlify（無料枠） |
| 形式 | PWA（Service Worker・manifest.json） |

---

## 画面構成

### セットアップ画面

- アプリ初回起動時または退出後に表示
- 名前を入力する（最大10文字）
- 「新しいリストを作成」→ 4文字の共有コードが自動生成される
- 「参加する」→ 共有コードを入力してパートナーのリストに参加する
  - 共有コード入力欄はEnterキーを無効化（誤送信防止）
  - 参加はボタン押下のみで実行

### メイン画面

ヘッダー・追加エリア・タグフィルター・セクションタブ・リストの5層構造。

---

## ヘッダー

- 左側：現在選択中のセクション名をフル表示
- 右側：同期ステータスバッジ（同期中 / 更新中 / オフライン）
- 右端：ハンバーガーメニューボタン

---

## ハンバーガーメニュー（ドロワー）

右からスライドインして表示される。

- **共有コード表示**：タップでクリップボードにコピー
- **ダークモード切り替え**：トグルで手動切り替え（システム設定を上書き）
- **店舗タグ管理**
  - 既存タグの一覧表示・削除（×ボタン）
  - タグ名を入力して「追加」ボタンで新規作成
  - タグ入力欄はEnterキーを無効化（ボタン押下のみで追加）
- **退出ボタン**：確認後にリストから退出しセットアップ画面に戻る

---

## アイテム追加エリア

2行構成。

### 1行目
- アイテム名入力欄
  - Enterキー単体では追加されない（PC誤操作防止）
  - Command+Enter（Mac）/ Ctrl+Enter（Windows）で追加
- 個数ステッパー（−／数字／＋）：デフォルト1

### 2行目
- メモ入力欄（サイズ・ブランドなど任意）
- 「追加」ボタン（グリーン背景・白文字）：押下で追加

---

## 店舗タグフィルターバー

- 店舗タグが1件以上登録されているときのみ表示
- 「すべて」ピルで全件表示
- タグをタップで絞り込み（再タップで解除）
- セクションタブの上部に配置

---

## セクションタブ

追加エリアの下部に横スクロールで表示。

- タブ名は最大5文字表示（超過は「...」で省略）
- 現在選択中のタブはハイライト表示
- タブ長押しでセクション編集シートを開く
- 右端の「＋」ボタンで新しいセクションを追加

### セクション仕様
- セクション名・順序・タイムスタンプをFirebaseに保存
- パートナーと共有される（リアルタイム同期）
- 初回作成時は「リスト」というデフォルトセクションが自動生成
- 最後のセクションは削除不可
- 削除時は対象セクションのアイテムを別セクションに移動

---

## アイテムリスト

### 通常状態アイテム

各アイテムの表示内容：
- 左：チェックボックス（丸型）
- 中央：アイテム名（太字）
- サブ行：個数・メモ（斜体）・店舗タグバッジ（オレンジ）
- 下段：「〇〇 が追加」（追加者名）
- 右：追加者アバター（自分＝青、パートナー＝緑）・削除ボタン（×）

### 操作

| 操作 | 結果 |
|------|------|
| チェックボックスをタップ | 「カゴに入れた」状態に移行 |
| 削除ボタンをタップ | 即時物理削除 |
| アイテムを長押し（500ms） | アイテム編集シートを開く |

### カゴに入れた状態アイテム

- チェックボックスがグリーンに
- アイテム名に打ち消し線・グレー表示
- 全体が少し薄く表示（opacity）
- チェックボックスを再タップで通常状態に戻る

---

## カゴに入れたセクション

「カゴに入れた」アイテムが1件以上あるときに表示。

- **全解除ボタン**：全件を通常状態に戻す（次の買い物前のリセット用）
- **購入ボタン**：押下で確認シートを表示し、OKで全件物理削除

---

## 進捗バー

- リスト上部に「完了数 / 全体数」とバーで表示
- 完了数に応じてグリーンのバーが伸びる

---

## アイテム編集シート（長押しで開く）

通常状態のアイテムのみ編集可能（カゴ内は不可）。

編集できる項目：
- アイテム名
- 個数（ステッパーで増減）
- メモ
- 店舗タグ（「なし」または登録済みタグから選択）

「完了」で保存・「キャンセル」で破棄。

---

## データ構造（Firebase）

```
rooms/
  {roomId}/
    sections/
      {sectionId}: { name, order, ts }
    items/
      {itemId}: { name, qty, memo, done, doneBy, by, ts, sectionId, tag }
    storeTags/
      {tagId}: { name, ts }
```

---

## 同期仕様

- Firebase Realtime DatabaseのonValueリスナーでリアルタイム同期
- セクション・アイテム・店舗タグそれぞれ独立してリスナーを張る
- 同期ステータスをヘッダーバッジでリアルタイム表示
- オフライン時はバッジが赤くなりトーストで通知

---

## PWA仕様

- `manifest.json`：アプリ名・アイコン・テーマカラーを定義
- `sw.js`：Service Workerでオフラインキャッシュ対応
- アイコン：`512-icon.png`（ショッピングバッグ＋チェックマークデザイン）
- iPhoneのSafariで「ホーム画面に追加」でアプリとして起動可能

---

## UI・UX仕様

### デザイン
- ライト・ダークモード対応（システム設定 or 手動切り替え）
- iPhoneのsafe-area（ノッチ・ホームインジケーター）に対応
- 最大幅430px、PC表示では中央寄せ
- ズーム防止（`user-scalable=no`・input要素のfont-size 16px強制）

### 操作
- 長押し判定：500ms
- 日本語キーボードの確定Enterによる誤操作を`isComposing`で防止
- ドロワー・シートはオーバーレイをタップで閉じる
- 共有コードはタップ1回でクリップボードコピー

### トースト通知
- 操作結果（追加・更新・削除・コピー等）を画面下部にトーストで表示
- 約2.6秒で自動消去

---

## セッション管理

- `localStorage`に名前・ルームIDを保存
- 次回起動時に自動ログイン
- 退出時に`localStorage`を削除

---

## 未実装（今後の候補）

- プッシュ通知（パートナーがアイテム追加時など）
- アイテムのドラッグ＆ドロップ並び替え
- セクション間のアイテム移動
